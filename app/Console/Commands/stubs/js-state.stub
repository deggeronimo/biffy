'use strict';

angular.module('biffyApp')
    .config(function ($stateProvider) {
        $stateProvider.state('{{state}}', {{{parent}}
            url: '',
            views: {
                '@': {
                    templateUrl: '{{template}}',
                    controller: '{{controller}}'
                }
            }
        });
    });